# npm workspaces installs node_modules at the root
node_modules/

# hono-bbs-admin is its own separate git repository, kept out of scope for this monorepo.
# It is only sitting in this directory for convenience; consider moving it elsewhere.
hono-bbs-admin/

# logs
*.log
npm-debug.log*

# Cloudflare API token credentials (see .cloudflare.env.example; never commit this)
.cloudflare.env

# misc
.DS_Store

# claude (project-specific instructions, not for OSS repo)
CLAUDE.md

# design directory
example_design/

# scratch/output files that shouldn't go to GitHub (Claudeとの検討用の下書き置き場)
local-output/
